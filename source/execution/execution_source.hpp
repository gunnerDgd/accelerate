#pragma once
#include <memory/memory_model.hpp>

#include <string>
#include <fstream>

namespace accelerate::execution {
	class source
	{
	public:
		source(memory::io_map::handle&);

	private:
		
	};
}